var bowling = function(){
    this.rolls = [];
};

function bowling(pins){
    this.rolling.push(pins);
    
};

function score(){
    var result = 0;
    var throws = 0;
    var game = this;
    
    for(var frame = 0; frame<10; frame++){
        if(isSpare()){
            result+=
        }
        
        else{
            result += this.rolls[throws] + this.rolls[throws + 1];
        }
        throw += 2;
    }
    
    return result; 
};

function isSpare(){
    
};
